# Base URL for local development
BASE_URL="http://localhost:3000"

###### DATABASE ######
# Set up a free PostgreSQL database on Neon: https://console.neon.tech/
# The project uses the Neon serverless driver adapter.
# Copy your connection string here.
DATABASE_URL="postgresql://neondb_owner:[YOUR_NEON_PASSWORD]@[YOUR_NEON_HOST]/neondb?sslmode=require"
# Database type: "neon" (hosted) or "postgres" (self-hosted, e.g. docker compose)
DATABASE_TYPE="neon"

###### AUTH ######
# Generate your own Secret Key for encryption. If ommited, the default will be used:
# https://www.better-auth.com/docs/installation#set-environment-variables
BETTER_AUTH_SECRET="YOUR_BETTER_AUTH_SECRET"

# Dev-only email/password auth (SSO-free) for faster local development.
# Always forced OFF in production by the server.
ENABLE_EMAIL_PASSWORD_AUTH="false"
# Public (non-secret) flag to show/hide the email/password UI in development.
# Even if enabled in the UI, the server still enforces ENABLE_EMAIL_PASSWORD_AUTH and non-production.
NEXT_PUBLIC_ENABLE_EMAIL_PASSWORD_AUTH="false"

# Create GitHub OAuth App: https://www.better-auth.com/docs/authentication/github
GITHUB_CLIENT_ID="YOUR_GITHUB_CLIENT_ID"
GITHUB_CLIENT_SECRET="YOUR_GITHUB_CLIENT_SECRET"

# Create Google OAuth Credentials: https://www.better-auth.com/docs/authentication/google
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

###### AI (global, server-only) ######
# Select provider globally for all AI features (chat/theme/prompt)
AI_PROVIDER="google" # google | openai | openai-compatible | groq

# Google (if AI_PROVIDER=google)
GOOGLE_AI_MODEL="gemini-2.5-flash"
# Get a Google API Key: https://aistudio.google.com/apikey
GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY"

# OpenAI (if AI_PROVIDER=openai)
OPENAI_AI_MODEL="gpt-4o-mini"
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# OpenAI-compatible (if AI_PROVIDER=openai-compatible)
OPENAI_COMPATIBLE_AI_MODEL="YOUR_MODEL_ID"
AI_BASE_URL="https://your-provider.example.com/v1"
AI_API_KEY="YOUR_PROVIDER_KEY"
AI_PROVIDER_NAME="my-openai-compatible"
# Note: the selected model must support structured outputs (required for theme schema generation).

# Groq (if AI_PROVIDER=groq)
GROQ_AI_MODEL="llama3-70b-8192"
# Get a Groq API Key: https://console.groq.com/keys
GROQ_API_KEY="YOUR_GROQ_API_KEY"
###### GOOGLE FONTS ######
# Get a Google Fonts API Key: https://developers.google.com/fonts/docs/developer_api
GOOGLE_FONTS_API_KEY="YOUR_GOOGLE_FONTS_API_KEY"

###### LANGSMITH (Observability) ######
# Enable LangSmith tracing for LLM call observability
# Recommended: "true" for dev/staging, "false" or sampling for production
LANGSMITH_TRACING="false"

# LangSmith API Key: https://smith.langchain.com/settings
LANGSMITH_API_KEY="YOUR_LANGSMITH_API_KEY"

# Project name for grouping traces in LangSmith dashboard
LANGSMITH_PROJECT="tweakcn"

# API endpoint (default US, use EU endpoint if needed)
# LANGSMITH_ENDPOINT="https://api.smith.langchain.com"
